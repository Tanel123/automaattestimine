package utility;

import java.util.Scanner;

public class CityNameScanner {
	
	private static Scanner scanner;
	
	//ma ei tohi testides kasutada seda meetodit, uuri lisa, vb konstruktor teha
	public static String cityNameScanner(){
		System.out.println("To insert city name from console insert 'C', To read city name from input.txt file insert 'R'");
		scanner = new Scanner(System.in);
		String decision = scanner.nextLine();
		if(decision.equalsIgnoreCase("C")){
			
			System.out.println("Enter city name: ");
			String cityname = scanner.nextLine();
			return cityname;
			
		}else if(decision.equalsIgnoreCase("R")){
			return null;
		}
		return null;
		
		//System.out.println("Your username is " + username);
	}
}



/*
System.out.println("Enter city name: ");
scanner = new Scanner(System.in);
String cityname = scanner.nextLine();
return cityname;
*/